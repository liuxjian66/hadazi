# 本地开发可复制为 .env；Render 上请在 Environment 页面填写同名变量
PORT=3000

# Supabase 项目地址，例如：https://xxxx.supabase.co
SUPABASE_URL=

# 推荐使用 service_role key，只放在后端环境变量里，不能暴露到前端页面
SUPABASE_SERVICE_ROLE_KEY=

# 如果只是本地演示，也可以不填 Supabase，后端会自动使用 data/db.json
